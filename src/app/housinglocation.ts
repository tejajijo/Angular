// interfaces define structure to the objects and creates blue print of the shape of the data
export interface Housinglocation {
    id: number;
    name: string;
    city: string;
    state: string;
    photo: string;
    availableUnits: number;
    wifi: boolean;
    laundry: boolean;
    
}
